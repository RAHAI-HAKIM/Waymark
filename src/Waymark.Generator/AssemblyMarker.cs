namespace Waymark.Generator;

/// <summary>
/// Names this assembly for the architecture tests. An assembly with no nameable
/// type makes every NetArchTest rule pass while inspecting nothing (decisions.md
/// D-006).
/// </summary>
public sealed class AssemblyMarker;
